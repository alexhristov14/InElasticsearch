# Default local 3-node cluster used when Server/Coordinator are started without an explicit
# cluster config path. Shard N is owned by nodes[N % nodes.size()].
totalShards=6
node-a=localhost:9091
node-b=localhost:9092
node-c=localhost:9093
